1. Create a Lovable CDN asset from the uploaded logo and store its pointer at `src/assets/logo.png.asset.json`.
2. Replace the leaf icon + text logo in `src/components/site/Header.tsx` with the uploaded image, sized to fit both desktop and mobile views within the `h-16` header.
3. Verify the preview renders correctly on both viewports.